meta {
  name: Crear Producto
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/api/productos
  body: json
  auth: inherit
}

body:json {
  {
    "nombre": "Teclado mecánico",
    "precio": 25000,
    "cantidad": 5,
    "descripcion": "Teclado RGB"
  } 
}

settings {
  encodeUrl: true
  timeout: 0
}
